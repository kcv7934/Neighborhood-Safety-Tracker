<h1>User Report Details</h1>

{{#if successMessage}}
  <p class="status-message" role="status">
    {{successMessage}}
  </p>
{{/if}}

<article class="report-details">
  <h2>{{report.category}}</h2>

  <p>
    <strong>Address:</strong>
    {{report.address}}
  </p>

  <p>
    <strong>Borough:</strong>
    {{report.borough}}
  </p>

  <p>
    <strong>Description:</strong>
    {{report.description}}
  </p>

  <p>
    <strong>Status:</strong>
    {{report.status}}
  </p>

  <p>
    <strong>Created:</strong>
    {{report.createdAt}}
  </p>

  <p>
    <strong>Last Updated:</strong>
    {{report.updatedAt}}
  </p>
</article>

<div class="page-actions">
  {{#if isOwner}}
    <a
      class="button-link"
      href="/saved-locations/create?userReportId={{report._id}}"
    >
      Save Location
    </a>
    <a class="button-link" href="/user-reports/{{report._id}}/edit">Edit Report</a>

    <button
      type="button"
      class="delete-button"
      id="delete-user-report-button"
      value="{{report._id}}"
    >Delete Report</button>
  {{/if}}

  <button type="button" id="back-button">
    Back
  </button>
</div>

{{#if isOwner}}
  <div id="delete-message" class="form-message" role="alert" hidden></div>
{{/if}}